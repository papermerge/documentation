# Groups

A group is a set of users.
One peculiar aspect about groups in {{ extra.project }} is that they may
have special folders: `home` and `inbox`. If you want
to use [group ownership](./group-ownership.md) feature then you need to create
group with special folders and include all team in that group.